<div>
  {{#if @headline}}
    <h1 typography:class="display">
      {{#if @label}}
        <small typography:class="small">
          {{@label}}
          <span typography:class="hidden">
            :
          </span>
        </small>
      {{/if}}
      {{@headline}}
    </h1>
  {{/if}}
  {{yield}}
</div>