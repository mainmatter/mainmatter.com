---
layout: base
title: "The C to Rust Migration Book"
description: "Learn how to migrate a production C codebase to safe, idiomatic Rust one component at a time."
permalink: /c-to-rust-migration-book/
hideNav: true
minimalFooter: true
og:
  image: /assets/images/rust/og-image.png
---

{% from "btn-primary.njk" import btnPrimary %}
{% from "btn-secondary.njk" import btnSecondary %}
{% from "strategy-list.njk" import strategyList %}
{% from "author-socials.njk" import authorSocials %}
{% from "color-hero.njk" import colorHero %}

{%
  set toc = {
    'Overview': '#overview',
    'The Book': '#course',
    'Table of contents': '#toc',
    'About the Author': '#author',
    'About Mainmatter': '#mainmatter'
  }
%}

<a href="/" class="c-to-rust-migration-logo">
  {% include 'svg/logo.njk' %}
  <span class="screenreader">Mainmatter</span>
</a>

{%
  set 'heroContent' = {
  "title": "The C to Rust Migration Book",
  "image": "/assets/images/hero/c-to-rust-lp.jpg",
  "text": "By Mainmatter",
  "alt": "A close up view of a machine with gears",
  "loading": "eager",
  "class": "c-to-rust-migration-hero",
  "toc": toc
  }
%}
{{ colorHero('purple', heroContent) }}

{#
<div class="c-to-rust-migration-hero">
  <a href="/" class="c-to-rust-migration-logo">
    {% include 'svg/logo.njk' %}
    <span class="screenreader">Mainmatter</span>
  </a>

  <div class="c-to-rust-migration-hero__image-wrapper">
    {% image "/assets/images/rust/c-to-rust-ferris.webp", "C to Rust Migration course graphic", "(min-width: 48em) 32vw, 80vw", "eager", "c-to-rust-migration-hero__image", [760, 1440] %}
  </div>

  <div class="c-to-rust-migration-hero__container container container--lg">
    <div class="c-to-rust-migration-hero__main">
      <h1 class="c-to-rust-migration-hero__title">C to Rust Migration</h1>
      <h3 class="c-to-rust-migration-hero__text">by Mainmatter</h3>
    </div>
  </div>
</div>
#}

<main class="c-to-rust-migration">
  <section id="overview" class="container container--lg my-10">
    <h3 class="mb-2">You don't have to rewrite everything</h3>
    <p>
      Most Rust migration guides tell you to start fresh. That's not how it works in practice. Real
      codebases are large, deployed, and depended on. You need a strategy that lets C and Rust
      coexist while you migrate piece by piece.
    </p>
    <p>
      That's what this guide covers. You'll learn how to bridge C and Rust through FFI, expose clean
      Rust APIs to existing C consumers, and progressively replace C with safe, idiomatic Rust,
      without breaking anything along the way.
    </p>
  </section>

  <section id="course" class="c-to-rust-migration-feature container container--xl my-10">
    <div class="c-to-rust-migration-feature__content">
      <h3>The C to Rust Migration Book</h3>
      {{
        btnPrimary({
          "label": "Read the book",
          "url": "/c-to-rust-migration-book/course"
        }, "white", "mt-2")
      }}
    </div>
    <div class="c-to-rust-migration-feature__image">
      {% image "/assets/images/rust/c-to-rust-books.jpg", "C to Rust migration books", "(min-width: 62em) 45vw, 90vw", "lazy", "c-to-rust-migration-feature__media", [760, 1440, 1920] %}
    </div>
  </section>

  <section class="container container--lg my-10">
    <p>
      Go through <a href="https://rust-exercises.com/">"The C to Rust Migration Book"</a> and learn
      how to bridge C and Rust through FFI, one concept at a time. You'll learn how languages
      communicate at the binary level, how to make that boundary safe and maintainable, and how to
      expose clean Rust APIs to existing C consumers.
    </p>
    <p>
      The guide assumes basic familiarity with both C and Rust. No prior FFI experience required;
      that's exactly what it teaches.
    </p>
    <p>
      You'll build up your knowledge in small, manageable steps. By the end, you'll have the tools
      and confidence to introduce Rust into a production C codebase incrementally, without
      disrupting what's already working.
    </p>
  </section>

  {% include "global/c-to-rust-newsletter-cta.njk" %}

  {%
    set topics = [{
    "title": "Foreign Function Interfaces",
    "text": "We kick off with the fundamentals of FFI: how C and Rust communicate at the binary level through the C ABI, symbol tables, and the linker. You'll learn why C became the universal standard for language interoperability and write your first <code>extern \"C\"</code> blocks by hand. You'll understand exactly what you're promising the compiler when you declare a binding — and what happens when you get the signature wrong."
    },{
    "title": "Types and Data Across the Boundary",
    "text": "This module covers how to safely move primitive values, strings, structs, and collections across the language boundary while preserving clear ownership semantics. You'll use <code>bindgen</code> to automate the generation of Rust bindings from C headers, and <code>cbindgen</code> to generate C headers from your Rust code, and learn how to configure both for a real codebase with non-trivial types."
    },{
    "title": "FFI-Safe Types",
    "text": "Raw pointers and untyped primitives are the biggest source of bugs at the FFI boundary. This module introduces safer alternatives: <code>NonNull</code>, <code>Option</code>, newtype wrappers, and enum-based discriminants that carry meaning instead of just values. You'll learn to design types that make invalid states unrepresentable, even when talking to C code that has no type safety of its own."
    },{
    "title": "Validation and Narrowing",
    "text": "Every FFI function is a trust boundary. C callers can pass null pointers, out-of-range values, or invalid UTF-8 — and Rust has no way to know at compile time. This module builds a discipline around validating and narrowing inputs at the boundary before they reach safe Rust, so that garbage data from C can never propagate into your business logic."
    },{
    "title": "Error Handling and API Design",
    "text": "Rust's <code>Result</code> and <code>?</code> stop at <code>extern \"C\"</code>. This module covers how to convert Rust's error types into C-compatible results: error code enums, out-parameters, and how to surface meaningful failure information without leaking Rust internals. You'll also learn to expose coarse operations rather than fine-grained setters and keep your API surface intentionally small."
    },{
    "title": "Production-Quality FFI",
    "text": "The final module ties everything together. You'll document FFI functions using Rust's <code>/// # Safety</code> convention with numbered invariants, so every unsafe contract is explicit and surfaces as C doc comments in the generated headers via <code>cbindgen</code>. We cover build integration, automated checks, and the mindset of treating your FFI surface as an API you'll maintain forever."
    }]
  %}
  <div id="toc">{{ strategyList('course-topic', 'Table of contents', topics) }}</div>

  {% set mentor = 'JonasKruckenberg' | findBySlug %}
  {% set mentorBio = "Jonas Kruckenberg is an engineer at Mainmatter and creator of the k23 operating system. He has helped people to build fast, secure, cross-platform desktop apps through his work on Tauri and is deeply involved in open-source, local-first, and humane software. Outside of code, he sings in classical ensembles and enjoys hiking." %}
  <section id="author" class="workshop-authors container container--lg">
    <div class="workshop__content post__content--workshop">
      <h3 class="workshop-topics__heading">About the author</h3>
      <div class="author__wrapper">
        <div class="author">
          <div class="workshop-authors__image-wrapper author__image-wrapper">
            {% image "/assets/images/authors/JonasKruckenberg.jpg", mentor.data.name, '3.75rem', 'eager', 'author__lead-image', [120] %}
          </div>
          <div class="author__text-wrapper">
            <div class="author__name-wrapper">
              <h4 class="author__name">{{ mentor.data.name }}</h4>
              {{ authorSocials('JonasKruckenberg') }}
            </div>
            <p class="author__title large">{{ mentor.data.title }} @Mainmatter</p>
          </div>
        </div>
        <div class="author__details mt-3 author__details--full-width">
          <p>{{ mentorBio or mentor.data.longBio }}</p>
        </div>
      </div>
    </div>
  </section>

  <section id="mainmatter" class="py-7 background-aqua">
    <div class="container mainmatter-section container--xl">
      <h3 class="mb-2">Need help with your Rust challenges? Reach out!</h3>
      <p>
        <a href="/">Mainmatter</a> is a software consulting company that specializes in Rust. We
        offer consulting services, training, and mentorship to companies that want to migrate from C
        to Rust or introduce Rust into an existing codebase.
      </p>
      <p>
        We can help with
        <a href="/rust-consulting/"
          >complex C-to-Rust migrations, reliability issues, training, and skill gaps.</a
        >
      </p>
      {%
        set link = {
        "label": "Contact us",
        "url": "/contact/?utm_source=C+Rust+Book"
        }
      %}
      {{ btnSecondary(link, "mt-2") }}
    </div>
  </section>

  <section class="c-to-rust-migration-eurorust container container--xl my-10">
    <div class="c-to-rust-migration-eurorust__image">
      {% image "/assets/images/rust/eurorust-2026.png", "EuroRust 2026 logo", "36rem", "lazy", "", [760, 1440] %}
    </div>
    <div class="c-to-rust-migration-eurorust__content">
      <p class="text--accent">Community</p>
      <h3 class="h3">EuroRust 2026</h3>
      <p>
        EuroRust is a two-day conference for all things Rust: two full conference days and two days
        of fun side activities, workshops, hackathons and more. Join us in October in Barcelona!
      </p>
      {{
        btnPrimary({
          "label": "Get your ticket",
          "url": "https://www.tickettailor.com/events/eurorust/2022388"
        }, "white", "mt-2")
      }}
    </div>
  </section>
</main>
